# Copy this file to .env and fill in your keys. Never commit .env.

# Natural voice (OpenAI TTS). Leave empty to use browser speech.
OPENAI_API_KEY=


# TTS: gpt-4o-mini-tts = expressive (supports instructions); tts-1/tts-1-hd = simpler
# TTS_VOICE=shimmer (higher-pitched), nova, alloy, coral
# TTS_INSTRUCTIONS= custom tone (only for gpt-4o-mini-tts); default adds warmth and variation
