{
  "name": "vox-scholar",
  "version": "1.0.0",
  "description": "Vox Scholar - Oral exam tutor with backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently -n api,web -c blue,green \"node server.js\" \"npm run dev:client\"",
    "dev:server": "node server.js",
    "build": "cd client && npm install --include=dev && npm run build",
    "dev:client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^11.6.0",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
